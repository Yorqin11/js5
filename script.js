const name = prompt('Ismingizni kiriting'); // Prompt for name
const id = prompt('Space idingizni kiriting'); // Prompt for ID
const password = prompt('Space passwordingizni kiriting'); // Prompt for password

const user = {
    name: name,
    id: id,
    password: password
};

console.log(user);